import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#19A7CE",
      light: "#97D9FF",
      dark: "#146C94",
    },
    info: {
      main: "#146C94",
    },
  },
});

export default theme;
